<view class="shell">
  <view class="shell_one" wx:for="{{docse}}" wx:key="{{index}}">
    <view>
      <image class="imgs" src="{{'https://statics.zhuishushenqi.com'+item.author.avatar}}"></image>
    </view>
    <view>
    <view class="nickname">{{item.author.nickname}}</view>
    <view class="evaluate">{{item.content}}</view>
  </view>
  </view>
</view>